#include <stdio.h>
int main ()
{

float time, distance, avgspeed ;

printf ("Enter The distance ", distance) ;
scanf ("%f", &distance) ;
printf ("Enter The time ", time) ;
scanf ("%f", &time);

avgspeed = distance/time ;
printf ("Average speed is %.2f ms-1", avgspeed) ;

}
